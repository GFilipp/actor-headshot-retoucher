# Core pipeline (required). Lower bounds are the tested floor; pin exactly in a
# lockfile for reproducible production installs.
numpy>=1.24
pillow>=10.0
opencv-python-headless>=4.8
scikit-image>=0.21

# Optional generator backend (only for real OpenAI runs; --dry-run needs none):
# openai>=1.0
