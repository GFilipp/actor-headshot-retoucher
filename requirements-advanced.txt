# Optional: stronger masks and quality gates. The pipeline runs without these
# and reports any gate it had to skip. These pull in heavy deps (torch, onnx)
# and can lag the newest Python releases.
-r requirements.txt

insightface>=0.7     # ArcFace identity-preservation gate
onnxruntime>=1.16    # insightface runtime
lpips>=0.1           # perceptual untouched-region gate (needs torch)
rawpy>=0.19          # RAW input support
